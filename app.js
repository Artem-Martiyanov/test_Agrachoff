const thumb = document.querySelector('.range-slider__input')const fillLine = document.querySelector('.range-slider__fill-line')const inputNumber = document.querySelector('.calc-form__input-number')const menu = document.querySelector('.nav__item--menu')const introBg = document.querySelector('.section__intro')const activateMenu = (e) => e.currentTarget.classList.toggle('active')function rangeHandler() {    inputNumber.value = thumb.value    fillLine.style.width = (thumb.value - thumb.min) * (100 / (thumb.max - thumb.min)) + '%'}rangeHandler()thumb.addEventListener('input', rangeHandler)menu.addEventListener('click', activateMenu)window.addEventListener('scroll', (e) => {   introBg.style.backgroundPosition = 100 + window.scrollY/5 + '%'})